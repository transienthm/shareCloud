http://blog.csdn.net/u011296485/article/details/74611183

## 多叉树基础

### 树节点的定义
```
  import lombok.Dsata;

  @Data
  public class TreeNode {
    private int parentId;
    private int selfId;
    protected String nodeName;
    protected Object obj;
    protected TreeNode parentNode;
    protected List<TreeNode> childList;

    public TreeNode(){
      initChild();
    }

    public void initChild(){
      if(childList == null){
        childList = new ArrayList<>();
      }
    }

    public boolean isLeaf(){
      if(childList == null){
        return true;
      }else{
        if(childList.isEmpty()){
          return true;
        }else{
          return false;
        }
      }
    }

    public void addChildNode(TreeNode treeNode){
      initChild();
      childList.add(treeNode);
    }

    public List<TreeNode> getElders() {
      List<TreeNode> list = new ArrayList<>();
      TreeNode parentNode = this.getParentNode();
      if(parentNode == null){
        return list;
      }else{
        list.add(parentNode);
        list.addAll(parentNode.getElders());
        return list;
      }
    }  


    public List<TreeNode> getJuniors(TreeNode treeNode){
      List<TreeNode> juniors = new ArrayList<>();
      List<TreeNode> childList = this.getChildList();
      if(CollectionUtils.isEmptyList(childList)){
        return juniors;
      }else{
        juniors.addAll(childList);
        for(TreeNode node : childList){
          juniors.addAll(getJuniors(node));
        }
      }
      return juniors;
    }




  }
```
